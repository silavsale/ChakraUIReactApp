import { Heading, Text, Box, Flex, FormControl, FormHelperText, FormLabel, Input, Button, FormErrorMessage, Container } from '@chakra-ui/react'
import './App.css'
import LoginPage from './components/LoginPage'

function App() {
  return (
    <Container>
      <LoginPage />
    </Container>
  )
}

export default App
